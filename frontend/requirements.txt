# Deployment requirements for the Streamlit frontend + agents. Deliberately
# separate from the root requirements.txt: the frontend doesn't need the
# data generator's python-snappy (which needs a system libsnappy), so this
# keeps the deployed image smaller and its build simpler.
#
# Versions are pinned EXACTLY, not with >= bounds: a floating bound let a
# fresh Cloud Build install resolve to brand-new major versions of
# google-adk and mcp that broke compatibility with each other (mcp 2.x
# restructured its module layout in a way google-adk 1.14.1 doesn't expect).
# Pin to what's actually been tested end-to-end.
google-cloud-aiplatform[agent_engines,adk]==1.165.1
google-adk==1.14.1
google-genai==1.75.0
mcp==1.30.0

requests==2.34.2
python-dotenv==1.2.3

streamlit==1.63.0
streamlit-autorefresh==1.0.1
plotly==7.0.0
